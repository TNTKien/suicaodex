import { Card, CardBody } from "@heroui/react";

export default function NoTitle() {
  return (
    <Card radius="sm" shadow="sm">
      <CardBody className="items-center">Chưa có gì ở đây cả!</CardBody>
    </Card>
  );
}
